import React from 'react';
import ReactDOM from 'react-dom/client';
import ChuckApp from './ChuckNorris';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <div>
      <ChuckApp/>
  </div>
);
